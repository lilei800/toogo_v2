# 市场状态映射一致性分析

## 📋 问题
机器人列表中的"风险配置"功能和创建机器人时的"静态映射"是否一致？

## 🔍 对比分析

### 1. 创建机器人页面 (`create.vue`)

**市场状态定义：**
```typescript
const marketStateMapping = [
  { key: 'trend', label: '趋势市场', icon: '📈', tagType: 'success' },
  { key: 'volatile', label: '震荡市场', icon: '📊', tagType: 'warning' },  // ⚠️ 使用 volatile
  { key: 'high_vol', label: '高波动市场', icon: '⚡', tagType: 'error' },
  { key: 'low_vol', label: '低波动市场', icon: '😴', tagType: 'info' },
];
```

**默认映射：**
```typescript
marketRiskMapping: {
  trend: 'balanced',        // 📈 趋势市场 → 平衡
  volatile: 'balanced',     // 📊 震荡市场 → 平衡  // ⚠️ 使用 volatile
  high_vol: 'aggressive',   // ⚡ 高波动市场 → 激进
  low_vol: 'conservative',  // 😴 低波动市场 → 保守
}
```

**风险偏好选项：**
```typescript
const riskPreferenceSelectOptions = [
  { label: '🛡️ 保守', value: 'conservative' },
  { label: '⚖️ 平衡', value: 'balanced' },
  { label: '🚀 激进', value: 'aggressive' },
];
```

---

### 2. 风险配置组件 (`RiskPreferenceConfig.vue`)

**市场状态定义：**
```typescript
const marketStates = [
  { key: 'trend', label: '趋势市场', icon: '📈', tagType: 'success' },
  { key: 'range', label: '震荡市场', icon: '📊', tagType: 'warning' },  // ⚠️ 使用 range
  { key: 'high_vol', label: '高波动市场', icon: '⚡', tagType: 'error' },
  { key: 'low_vol', label: '低波动市场', icon: '😴', tagType: 'info' },
];
```

**默认映射：**
```typescript
marketRiskMapping: {
  trend: 'balanced',        // 📈 趋势市场 → 平衡
  range: 'balanced',        // 📊 震荡市场 → 平衡  // ⚠️ 使用 range
  high_vol: 'aggressive',   // ⚡ 高波动市场 → 激进
  low_vol: 'conservative',  // 😴 低波动市场 → 保守
}
```

**风险偏好选项：**
```typescript
const riskPreferenceOptions = [
  { label: '🛡️ 保守 - 低杠杆、小仓位、严格止损', value: 'conservative' },
  { label: '⚖️ 平衡 - 中等杠杆、适中仓位、标准止损', value: 'balanced' },
  { label: '🚀 激进 - 高杠杆、大仓位、宽松止损', value: 'aggressive' },
];
```

---

## ❌ 发现的不一致问题

### 问题1：市场状态 key 不一致

| 位置 | 震荡市场的 key |
|------|---------------|
| 创建机器人页面 | `volatile` |
| 风险配置组件 | `range` |

**影响：**
- 创建机器人时保存的是 `volatile: 'balanced'`
- 风险配置组件显示的是 `range: 'balanced'`
- 两者无法正确对应，导致数据不一致

### 问题2：风险偏好选项描述不一致

| 位置 | 描述格式 |
|------|---------|
| 创建机器人页面 | 简短：`🛡️ 保守` |
| 风险配置组件 | 详细：`🛡️ 保守 - 低杠杆、小仓位、严格止损` |

**影响：**
- 用户体验不一致
- 但功能上无影响（value 都是 `conservative`）

---

## ✅ 一致的部分

1. **市场状态数量**：都是 4 个（trend, 震荡, high_vol, low_vol）
2. **风险偏好选项**：都是 3 个（conservative, balanced, aggressive）
3. **默认映射值**：除了震荡市场的 key 不同，其他映射值都一致
4. **数据结构**：都使用 `Record<string, string>` 存储映射关系

---

## 🔧 需要修复的问题

### 修复方案：统一使用 `volatile` 作为震荡市场的 key

**原因：**
1. 创建机器人时已经使用 `volatile`
2. 后端代码中可能已经使用 `volatile`
3. 需要保持向后兼容

**修改位置：**
- `web/src/views/toogo/robot/components/RiskPreferenceConfig.vue`
  - 将 `range` 改为 `volatile`

---

## 📊 总结

| 对比项 | 创建机器人页面 | 风险配置组件 | 是否一致 |
|--------|--------------|------------|---------|
| 市场状态 key | `volatile` | `range` | ❌ **不一致** |
| 风险偏好 value | `conservative/balanced/aggressive` | `conservative/balanced/aggressive` | ✅ 一致 |
| 默认映射值 | `volatile: 'balanced'` | `range: 'balanced'` | ❌ **不一致** |
| 数据结构 | `Record<string, string>` | `Record<string, string>` | ✅ 一致 |
| 风险偏好选项描述 | 简短 | 详细 | ⚠️ 不一致（不影响功能） |

**结论：** ❌ **不完全一致**，主要问题是震荡市场的 key 不同（`volatile` vs `range`），这会导致数据无法正确对应。

